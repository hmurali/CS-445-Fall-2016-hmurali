How to build each program:
1. run the MAKEFILE 'makefile.mak' 
2. running the file from step 1 will compile the programs included in this
3. IMPORTANT: run 'make' command to compile and build java source files.
zip file when executed. 

How to run each program:
1. Run the TestCreature.java file, it contains the test cases for 
this homework assignment. 
2. Use the unit tests to run each program. 